{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "dsh-supervisor 壳主窗口基础能力（core:default）；壳自有命令由 main.rs 的 invoke_handler 注册",
  "windows": ["main"],
  "permissions": [
    "core:default"
  ]
}
